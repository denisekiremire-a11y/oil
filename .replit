run = "npm install && npm start"
entrypoint = "server/src/index.js"

[env]
NODE_ENV = "production"

[[ports]]
localPort = 3001
externalPort = 80

[deployment]
build = ["sh", "-c", "npm install && npm run build -w client"]
run = ["sh", "-c", "npm run start -w server"]
